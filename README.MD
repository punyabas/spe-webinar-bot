#Contoh Bot Webinar SPE

* Clone this project
* Run ``` composer install ```
* create bot tele from bot father
* enable setting inline mode and group privacy mode 
* insert your bot token and selected group on handler class
* run cron.php
